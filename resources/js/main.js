
// If user clicks on button, we want what they typed to the 'todo' list
document.getElementById("addTask").addEventListener('click', function() {
    var value = document.getElementById('item').value;
    if (value){

        /*todo error check for if the list is empty*/

    }

});